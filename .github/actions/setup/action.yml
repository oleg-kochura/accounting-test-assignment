name: Setup
description: Install Node.js (from .nvmrc) and project dependencies

runs:
  using: composite
  steps:
    - uses: actions/setup-node@v7
      with:
        node-version-file: .nvmrc
        cache: npm

    - run: npm ci
      shell: bash
      env:
        HUSKY: 0
